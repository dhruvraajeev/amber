# Copy to .env and fill in locally. Never commit .env.
# Values go in .env, GitHub secrets, or Azure secrets — never in chat or code.

# Runtime environment (e.g. local, prod)
AMBER_ENV=

# Step 29 — NVIDIA NIM (free credits)
NIM_API_KEY=

# Step 30 — MongoDB Atlas M0 (free)
MONGODB_URI=

# Step 31 — Application Insights
OTEL_EXPORTER_CONNECTION_STRING=
