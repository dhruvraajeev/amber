{
  "description": "6,000 req/s, over the 5,000 limit (10 s keeps requests under 200k).",
  "expect": [
    "LIMIT_RATE"
  ],
  "config": {
    "durationS": 10,
    "seed": 42,
    "warmupS": 5
  },
  "design": {
    "name": "limit-rate",
    "version": 1,
    "nodes": [
      {
        "id": "users",
        "kind": "users",
        "label": "users",
        "position": {
          "x": 200,
          "y": 0
        },
        "params": {
          "traffic": {
            "type": "constant",
            "rps": 6000
          },
          "clientTimeoutMs": 5000
        }
      },
      {
        "id": "api",
        "kind": "service",
        "label": "api",
        "position": {
          "x": 400,
          "y": 0
        },
        "params": {
          "replicas": 2,
          "concurrencyPerReplica": 50,
          "queueLimit": 200,
          "work": {
            "p50Ms": 20,
            "p99Ms": 100
          },
          "costPerReplicaMonth": 30
        }
      }
    ],
    "edges": [
      {
        "id": "e_users_api",
        "source": "users",
        "target": "api"
      }
    ]
  }
}
